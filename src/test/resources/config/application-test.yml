spring:
  datasource:
    driver-class-name: org.h2.Driver
    jdbc-url: jdbc:h2:mem:h2test;DB_CLOSE_DELAY=-1;MODE=MySQL
    username: sa
    password: sa
  h2:
    console.enabled: false

  # flyway
  flyway:
    enabled: false
    clean-disabled: true
    table: flyway_schema_history